name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Syntax Validation
      run: echo "Validating HTML..." 

    - name: Link Checking
      run: echo "Checking Links..."

    - name: Build Preview
      run: echo "Building Preview..."

    - name: Deployment
      run: echo "Deploying to GitHub Pages..."
